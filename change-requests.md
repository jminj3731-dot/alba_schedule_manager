# Change Requests

## Status Legend
- [ ] 논의 중
- [~] 승인됨 (미구현)
- [x] 완료

---

## 완료된 작업

### [x] 추가 버튼 DB 저장 오류 수정
- `DATABASE_URL` 환경변수 오류 (`${{MySQL.DATABASE_URL}}` → `${{MySQL.MYSQL_URL}}`)
- mysql2 URL 파싱 방식 변경 (수동 파싱으로 SSL 옵션 적용)
- Railway MySQL 테이블 마이그레이션 실행

---

## 새 요청사항

### [ ] 출퇴근 시간 수정 기능 (알바생 셀프 수정)

#### 배경
출퇴근 버튼을 늦게 누르거나 동시에 누르는 경우가 있어, 알바생이 직접 수정할 수 있는 흐름이 필요함.

#### 감지 조건 (팝업 트리거)
- 출근 체크인이 예정 출근 시간보다 **15분 이상** 늦은 경우 → 출근 버튼 처리 후 팝업
- 출근/퇴근 입력 간격이 **3분 이하**인 경우 (동시 누름) → 퇴근 버튼 처리 후 팝업
- 두 기준값 모두 상수값으로 하드코딩 (초기 버전, 관리자 설정 없음)

#### UI 흐름
1. 출퇴근 버튼 누름
2. 감지 조건 해당 시 팝업 표시:
   - 출근 늦음: `"출근 시간보다 늦게 입력되었어요. 실제 출근 시간으로 수정하시겠어요?"`
   - 동시 누름: `"출퇴근이 거의 동시에 입력되었어요. 실제 시간으로 수정하시겠어요?"`
3. 팝업 내 두 가지 선택:
   - **[수정하기]** → 시간 수정 폼 표시
   - **[그대로 저장]** → 팝업 닫고 현재 시간으로 기록 (스킵 로그 남김)
4. 시간 수정 폼:
   - 출근 늦음: 출근 시간 입력 1개
   - 동시 누름: 출근/퇴근 시간 입력 2개
   - 오늘 날짜 고정 (날짜 변경 불가 — 예외는 관리자 수정으로 처리)
   - 수정 사유 입력 **필수**
5. 저장 → DB에 수정 이력 기록 + 관리자 이메일 발송

#### DB
- 수정 이력 로그 테이블 (신규): 원래 시간, 수정된 시간, 사유, 수정 일시, 알바생 정보
- 스킵 로그도 동일 테이블에 기록 (type 구분)

#### 관리자 이메일 알림
- 수정 발생 시 이메일 발송
- 수신 이메일 우선순위:
  1. Settings 페이지에 저장된 관리자 이메일
  2. 환경변수 `ADMIN_NOTIFICATION_EMAIL` (fallback)
- 쉼표 구분 다중 수신자 지원
- 이메일 내용: 알바생 이름, 원래 시간, 수정된 시간, 사유

#### Settings 페이지
- 관리자 이메일 입력/수정 기능 추가 (Settings 페이지에 항목 추가)

#### 미루는 것 (추후 단계)
- 날짜 변경 기능
- 관리자 승인 플로우

#### 원칙
- 수정 시 사유 입력 필수
- 수정/스킵 모두 DB 로그 기록
- 수정 강제 없음 (스킵 가능)
